const {
  registerService,
  keepLoginService,
  verificationService,
  loginService,
} = require("./authServices");
module.exports = {
  registerService,
  keepLoginService,
  verificationService,
  loginService,
};
